1java数组的写法
比如有一个Student类
Student[] stu = new Student[]{...}; 
静态初始化由我们给定长度 动态初始化由程序员给定初始化值
String str = new String[]{};

二维数组 String[][] str = new String[][]{{},{}};
         int[][] x = new int[3[3] 静态初始化 Student[] stus=new Student[5];
         int[][] x = new int[][]{{1,2,3},{4,5,6},{7,8,9}};
         
  Student stu = new Student();这是创建对象
  创建对象数组的时候
  Student[] stus = new Student[]{};
  
  注意区别,别搞了,创建对象很明显用(),创建数组的时候由于要申明长度后面先用[]申明长度再用{}装初始化数据
  
         
2javaScript数组

var array = new Array("","",""); 固定使用new Array来创建数组

var array = ["","",""]; 直接这样用中括号赋值 二维数组的话[[]] 中括号内嵌套中括号

3当我们在一个静态方法内使用本类的成员的时候,该成员必须是静态的,不管是变量,方法,还是内部类都必须是静态的
因为静态方法可能会被类名直接引用,那么在这种情况下类的其他成员就没有载入内存,那么在静态方法中使用就会找不到这些东西,所以
在静态方法中使用的成员必须静态,前提是使用同一个类中的成员,如果使用其他类中的东西,只需要new 类名() 然后引用即可,

4在for()循环中,如果要初始化多个值
记住这个初始化是要符合java规范的
比如  int i=0,j=0; 但是没有 int i=0,int j=0;这种写法 也没有int i=0,double j=0;
逗号不能隔开两个完成的赋值表达式 ,完整的表达式(类型 名字=值;)是由分号隔开的,比如int i=0;double j=0;
而这种(int i=0,j=0)他是一个表达式
for循环中第一个;内的初始化和 上述的规范一致!!!!

5Math.pow()返回的是double值
之所以不返回int 是因为担心损失精度
所以记住一般而言 想要将Math.pow()转为Long

java中两个相同类型的数运算,最后的结果一定是这个类型的,如果结果溢出了,
它会自动变成一个该类型以内的数字,这个数字是可以算出来的,通过反码补码计算


